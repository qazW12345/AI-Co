# bootstrap/build/lex.txt
# WP-M0-08 deterministic lexer area source list (owned by WP-M0-08).
# Consumed by build-stage0-msvc.cmd / build-stage0-clang.cmd fragment
# aggregation (CONVENTIONS.md §3). One source path per line, relative to the
# repository root.
#
# lex_test.c is intentionally NOT listed here: it is a unit-test program
# with its own main() and is built explicitly together with these library
# sources (see bootstrap/src/lex/README.md).
bootstrap/src/lex/lex.c
