# bootstrap/build/types_a.txt
# WP-M0-11a type identity and type tables area source list (owned by WP-M0-11a).
# Consumed by build-stage0-msvc.cmd / build-stage0-clang.cmd fragment
# aggregation (CONVENTIONS.md §3). One source path per line, relative to the
# repository root.
#
# types_test.c is intentionally NOT listed here: it is a unit-test program
# with its own main() and is built explicitly together with these library
# sources (see bootstrap/src/types/README.md).
bootstrap/src/types/type_identity.c
bootstrap/src/types/type_tables.c
