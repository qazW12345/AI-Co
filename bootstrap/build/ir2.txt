# bootstrap/build/ir2.txt
# WP-M0-16b2 IR deterministic dump and verification (contract
# docs/contracts/IR-CONTRACT-2026-08-12.md sec. 11).
# Area source list (owned by WP-M0-16b2). Consumed by
# build-stage0-msvc.cmd / build-stage0-clang.cmd fragment aggregation
# (CONVENTIONS.md §3). One source path per line, relative to the
# repository root. Supersedes the first-level ir.txt entry per manifest
# §5 (sub-split WP-M0-16b1/16b2).
#
# ir_dump.c depends on the WP-M0-16b1 IR core (bootstrap/build/ir1.txt)
# and the WP-M0-06 diag record/span model (bootstrap/build/diag.txt),
# both included by the aggregate build.
#
# ir_dump_test.c is intentionally NOT listed here: it is a unit-test
# program with its own main() and is built explicitly together with the
# library sources (see the build comment at the top of ir_dump_test.c).
bootstrap/src/ir/ir_dump.c
