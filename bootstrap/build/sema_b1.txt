# bootstrap/build/sema_b1.txt
# WP-M0-13b1 expression core and evaluation order
# area source list (owned by WP-M0-13b1). Consumed by
# build-stage0-msvc.cmd / build-stage0-clang.cmd fragment aggregation
# (CONVENTIONS.md §3). One source path per line, relative to the
# repository root.
#
# expr_core_test.c is intentionally NOT listed here: it is a
# unit-test program with its own main() and is built explicitly
# together with these library sources (see the build comment at the
# top of expr_core_test.c).
bootstrap/src/sema/expr_core.c
