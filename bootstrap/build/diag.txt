# bootstrap/build/diag.txt
# WP-M0-06 diagnostic infrastructure area source list (owned by WP-M0-06).
# Consumed by build-stage0-msvc.cmd / build-stage0-clang.cmd fragment
# aggregation (CONVENTIONS.md §3). One source path per line, relative to the
# repository root.
#
# diag_test.c is intentionally NOT listed here: it is a unit-test program
# with its own main() and is built explicitly together with these library
# sources (see bootstrap/src/diag/README.md).
bootstrap/src/diag/diag_codes.c
bootstrap/src/diag/diag.c
bootstrap/src/diag/diag_emit.c
