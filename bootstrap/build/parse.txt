# bootstrap/build/parse.txt
# WP-M0-09 parser area source list (owned by WP-M0-09).
# Consumed by build-stage0-msvc.cmd / build-stage0-clang.cmd fragment
# aggregation (CONVENTIONS.md §3). One source path per line, relative to the
# repository root.
#
# ast_test.c / parse_test.c are intentionally NOT listed here: they are
# unit-test programs with their own main() and are built explicitly together
# with these library sources (see bootstrap/src/parse/README.md).
bootstrap/src/parse/parse.c
