# bootstrap/build/rt_mem_a1.txt
# WP-M0-14a1 allocation registry and semantics (spec sec. 15.1,
# ADR-004). Area source list (owned by WP-M0-14a1). Consumed by
# build-stage0-msvc.cmd / build-stage0-clang.cmd fragment aggregation
# (CONVENTIONS.md §3). One source path per line, relative to the
# repository root.
#
# rt_mem_alloc_test.c is intentionally NOT listed here: it is a
# unit-test program with its own main() and is built explicitly
# together with the library source (see the build comment at the
# top of rt_mem_alloc_test.c).
bootstrap/runtime/rt_mem/rt_mem_alloc.c
