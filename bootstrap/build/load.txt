# bootstrap/build/load.txt
# WP-M0-07 source loader / UTF-8 validation area source list (owned by WP-M0-07).
# Consumed by build-stage0-msvc.cmd / build-stage0-clang.cmd fragment
# aggregation (CONVENTIONS.md §3). One source path per line, relative to the
# repository root.
#
# load_test.c is intentionally NOT listed here: it is a unit-test program
# with its own main() and is built explicitly together with these library
# sources (see bootstrap/src/load/README.md).
bootstrap/src/load/load.c
