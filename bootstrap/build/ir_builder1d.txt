# bootstrap/build/ir_builder1d.txt
# WP-M0-16c1d IR builder Phase B statement mapping/terminators area
# source list (owned by WP-M0-16c1d). Consumed by build-stage0-msvc.cmd
# / build-stage0-clang.cmd fragment aggregation (CONVENTIONS.md §3). One
# source path per line, relative to the repository root. Supersedes the
# first-level ir_builder.txt entry per manifest §5 (sub-split
# WP-M0-16c1a..16c1d; letter-suffix fragments ir_builder1a.txt..
# ir_builder1d.txt per amendment line 55).
#
# ir_builder_stmt.c depends on ir_builder_expr.c (bootstrap/build/
# ir_builder1c.txt), ir_builder_decl.c (bootstrap/build/ir_builder1b.txt),
# ir_builder_core.c (bootstrap/build/ir_builder1a.txt), the WP-M0-16b1 IR
# core (bootstrap/build/ir1.txt), the WP-M0-06 diag record/span model
# (bootstrap/build/diag.txt), the WP-M0-12 const evaluator
# (bootstrap/build/const_a.txt), and the WP-M0-10 name / WP-M0-11 type /
# WP-M0-09 AST packages; all are included by the aggregate build. The
# mapper installs the Phase B body mapper through the ir_builder_core.h
# seam and lowers statement nodes (contract §5.2, §5.6, §6.1, §12.1)
# into the function body IR_BLOCK created by 16c1b, consuming the 16c1c
# expression lowerer for every expression position.
#
# ir_builder_stmt_test.c and ir_builder_integration_test.c are
# intentionally NOT listed here: they are test programs with their own
# main() and are built explicitly together with the library sources
# (see the build comment at the top of each test file).
bootstrap/src/ir/ir_builder_stmt.c
