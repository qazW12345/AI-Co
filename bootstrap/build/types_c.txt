# bootstrap/build/types_c.txt
# WP-M0-11c implicit conversions and common type area source list (owned by
# WP-M0-11c). Consumed by build-stage0-msvc.cmd / build-stage0-clang.cmd
# fragment aggregation (CONVENTIONS.md §3). One source path per line,
# relative to the repository root.
#
# convert_test.c is intentionally NOT listed here: it is a unit-test
# program with its own main() and is built explicitly together with these
# library sources (see the build comment at the top of convert_test.c).
bootstrap/src/types/convert.c
