# bootstrap/build/const_a.txt
# WP-M0-12a constant-expression evaluator core area source list
# (owned by WP-M0-12a). Consumed by build-stage0-msvc.cmd /
# build-stage0-clang.cmd fragment aggregation (CONVENTIONS.md §3). One
# source path per line, relative to the repository root.
#
# eval_core_test.c is intentionally NOT listed here: it is a unit-test
# program with its own main() and is built explicitly together with
# these library sources (see the build comment at the top of
# eval_core_test.c).
bootstrap/src/const/eval_core.c
