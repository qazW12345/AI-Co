# bootstrap/build/types_d.txt
# WP-M0-11d explicit cast/wrap matrix and operator typing area source list
# (owned by WP-M0-11d). Consumed by build-stage0-msvc.cmd /
# build-stage0-clang.cmd fragment aggregation (CONVENTIONS.md §3). One source
# path per line, relative to the repository root.
#
# optype_test.c is intentionally NOT listed here: it is a unit-test program
# with its own main() and is built explicitly together with these library
# sources (see the build comment at the top of optype_test.c).
bootstrap/src/types/optype.c
