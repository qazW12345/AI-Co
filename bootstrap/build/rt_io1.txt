# bootstrap/build/rt_io1.txt
# WP-M0-15a1 rt.io core (spec sec. 15.2/15.8).
# Area source list (owned by WP-M0-15a1). Consumed by
# build-stage0-msvc.cmd / build-stage0-clang.cmd fragment aggregation
# (CONVENTIONS.md §3). One source path per line, relative to the
# repository root. Supersedes the first-level rt_io.txt entry per
# manifest §5 (sub-split WP-M0-15a1/15a2).
#
# rt_io_core.c depends on the WP-M0-06 diag record shape for the
# AIC-R0807 read-buffer trap record (bootstrap/src/diag/diag.h); the
# diag sources are listed in the WP-M0-06-owned fragment
# bootstrap/build/diag.txt, which the aggregate build includes.
#
# rt_io_core_test.c is intentionally NOT listed here: it is a
# unit-test program with its own main() and is built explicitly
# together with the library sources (see the build comment at the
# top of rt_io_core_test.c).
bootstrap/runtime/rt_io/rt_io_core.c
